{
  "name": "motionkit-workspace",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace motionkit-plugin",
    "build": "npm run build --workspace motionkit-plugin",
    "lint": "npm run lint --workspace motionkit-plugin",
    "pack": "npm run pack --workspace motionkit-plugin"
  }
}

